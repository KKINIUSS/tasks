#include "function.hpp"
#include <cstdlib>

bool large (int a, int b) {
    return a > b ? true : false;
}

bool less (int a, int b) {
    return a < b ? true : false;
}

bool large_equal (int a, int b) {
    return a >= b ? true : false;
}

bool less_equal (int a, int b) {
    return a <= b ? true : false;
}

int main() {
    return 0;
}
